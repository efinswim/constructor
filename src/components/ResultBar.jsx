import React from 'react';
import { Box } from '@mui/material';

function ResultBar() {
  return (
    <Box flex={3} p={2}>
      <Box position="fixed" width={300}></Box>
    </Box>
  );
}

export default ResultBar;
